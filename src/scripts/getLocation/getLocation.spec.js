import getLocation from './getLocation';

describe('getLocation', () => {
  describe('when getLocation is called with ... ', () => {
    it('returns ...', () => {
      expect(true).toEqual(true);
    });
  });
});
