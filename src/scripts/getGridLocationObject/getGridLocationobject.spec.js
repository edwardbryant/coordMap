import getGridLocationObject from './getGridLocationObject';

describe('getGridLocationObject', () => {
  describe('when provided no size param', () => {
    xit('returns the minimum', () => {
      expect(getQuadrantSize()).toEqual(0);
    });
  });
});
